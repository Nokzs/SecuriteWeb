server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Redirige toutes les requêtes vers index.html pour laisser React/Vite gérer le routage
        try_files $uri $uri/ /index.html;
    }
}
